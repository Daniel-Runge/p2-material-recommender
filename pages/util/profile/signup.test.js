const validate = require('./validate')

test('validates the password', () => {
    expect(sum().toBe())
  //const pass = '1Mnkopte';
  //expect(validates().toEqual())
  //expect(validates().not.toBe())
})