function calcProb4(person.undertanding)


/*We expect on the database to see how many users, which have the same FS profiles, have voted on a particular material */
/* */

function {

}

function k(material) {
    let percentage;

    percentage = percentagePositive(material.positive, material.negative);



}

function percentagePositive(positive, negative) {
    if (positive === null || negative === null) return;

    return (positive + negative) / positive;
}